# Fork Purpose

This fork exists temporarily to carry one local patch: discover Cursor and OpenCode
skills from the local machine and expose them through T3 Code's existing `$` skill
picker.

Keep this fork otherwise aligned with upstream `pingdotgg/t3code`. Once upstream
ships equivalent provider skill discovery, drop the local patch and return to
upstream.
